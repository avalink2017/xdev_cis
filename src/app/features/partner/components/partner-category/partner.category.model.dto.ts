export interface PartnerCategoryDTO {
  id: string;
  code: string;
  name: string;  
  concurrencyStamp: string;
}

export interface PartnerCategoryListDTO {
  id: string;
  code: string;
  name: string;
}
