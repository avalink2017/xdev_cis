export interface AssetCategoryDTO {
  id: string;
  code:string;
  name: string;
  rangeId:string;
  concurrencyStamp: string;
}

export interface AssetCategoryListDTO {
  id: string;
  code: string;
  name: string;
}
