export interface SignerDTO {
  id: string;
  code: string;
  name: string;
  title: string;
  concurrencyStamp: string;
}

export interface SignerListDTO {
  id: string;
  code: string;
  name: string;
  title: string
}
